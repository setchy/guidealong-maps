# Server-side environment variables for scripts in /scripts
# Do NOT commit your real key. Copy this file to .env and fill in your key.

# Google Maps Geocoding API key (server-side)
# Enable the Geocoding API in your Google Cloud project.
# Use a server-side key (no HTTP referrer restrictions). Prefer IP allow-listing.
GOOGLE_MAPS_API_KEY=
